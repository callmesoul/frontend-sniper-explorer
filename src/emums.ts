export enum Lang {
  CN = 'cn',
  EN = 'en'
}

export enum NFTIssueParamsIconType {
  File = 'pic',
  TXId = 'txId'
}

export enum SdkType {
  Null = -1,
  App = 1,
  Metaidjs = 2,
  Dotwallet = 3
}

export enum Encrypt {
  Yes = '1',
  No = '0'
}

export enum PayToAddressCurrency {
  SATS = 'sats',
  BSV = 'bsv'
}
export enum AppMode {
  PROD = 'prod',
  GRAY = 'gray',
  TEST = 'test',
  DEV2 = 'dev2'
}
